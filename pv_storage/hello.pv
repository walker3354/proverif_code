(* h e l l o e x t . pv : H e l l o Ex tended World S c r i p t *)

free c:channel.

free Cocks.bitstring [private].
free RSA:bitstring [private].

query attacker(RSA).
query attacker(Cocks).

process
    out ( c ,RSA ) ;
0